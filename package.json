{
  "name": "perfumery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "generate": "npm --prefix app run generate",
    "pipeline": "node main.js floral && node scripts/build-leaderboard.js"
  },
  "dependencies": {
    "stream-json": "^3.5.0"
  }
}
